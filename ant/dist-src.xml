<?xml version="1.0"?>

<project name="freeguide-tv-source" default="dist-src">
    
    <import file="plugins-info.xml"/>
    
    <target name="dist-src"
        description="generate the source tar.gz and zip">
        
        <mkdir dir="${dist-src}" />
        <tar longfile="gnu"
            destfile="${dist-src}/${application-id}-${application-version}.tar.gz"
            compression="gzip">
            
            <tarfileset dir="." prefix="${application-id}-${application-version}">
                <exclude name="**/.xvpics/**" />
                <exclude name="**/.svn/**"/>
                <exclude name="build/**"/>
                <exclude name="dist/**"/>
                <exclude name="stats/**"/>
                <exclude name="doc/nonlocal/**"/>
                <exclude name="doc/html-web/**"/>
                <exclude name="**/*.class"/>
            </tarfileset>
            
        </tar>
        
        <zip zipfile="${dist-src}/${application-id}-${application-version}.zip">
        
            <zipfileset dir="." prefix="${application-id}-${application-version}">
                <exclude name="**/.xvpics/**" />
                <exclude name="**/.svn/**"/>
                <exclude name="build/**"/>
                <exclude name="dist/**"/>
                <exclude name="stats/**"/>
                <exclude name="doc/nonlocal/**"/>
                <exclude name="doc/html-web/**"/>
                <exclude name="**/*.class"/>
            </zipfileset>
            
        </zip>
        
    </target>
</project>

