<?xml version="1.0"?>

<project name="freeguide-tv-dist-rpm" default="dist-rpm">

    <import file="compile.xml"/>
    <import file="dist-src.xml"/>

    <target name="dist-rpm" depends="prepare,dist-src">
        <mkdir dir="${dist-rpm}"/>
        <exec dir="${install}/linux/rpm" executable="buildrpm" resolveexecutable="true" failonerror="true"/>
    </target>

</project>

