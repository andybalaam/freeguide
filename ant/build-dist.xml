<?xml version="1.0"?>

<project name="freeguide-tv-dist">

	<import file="build-plugins-info.xml"/>

	<target name="dist-tgz">
		<mkdir dir="${dist-tgz}"/>
        
		<tar longfile="gnu"
			 destfile="${dist-tgz}/freeguide-${application-id}-${application-version}-bin.tar.gz" compression="gzip" >
            <tarfileset dir="${build}/package" prefix="/usr/share/freeguide/" />
        </tar>
	</target>
    
</project>

